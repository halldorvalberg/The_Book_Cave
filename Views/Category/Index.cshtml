@model List<The_Book_Cave.Models.ViewModels.CategoryViewModel>

<h2>Allir Bókaflokkar</h2>

<div>
    @foreach(var category in Model)
    {
        <a asp-controller="Category" asp-action="Details" asp-route-id="@category.Id">@category.Name</a><br />
    }
</div>
